spring.application.name=messenger

# JavaMailSender
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=philtpd10207@gmail.com
spring.mail.password=xfpk zviq llbn awxp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.thymeleaf.encoding=UTF-8

# Database configuration
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=MessengerAppFuture;encrypt=false;sendStringParametersAsUnicode=true;
spring.datasource.username=philt
spring.datasource.password=0000
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Hibernate configuration
# spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.show-sql=true

# Optional: Logging for debugging
logging.level.org.springframework.web=DEBUG
logging.level.poly.asm=DEBUG

# Cấu hình kích thước tối đa cho tải lên tệp
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cấu hình thư mục tải lên
spring.servlet.multipart.location=${java.io.tmpdir}

# Cấu hình thời gian chờ tải lên
spring.servlet.multipart.resolve-lazily=true




server.port=${PORT:8080}